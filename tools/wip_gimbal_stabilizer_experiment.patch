diff --git a/docs/changelog.md b/docs/changelog.md
index bed29db..f14bd5b 100644
--- a/docs/changelog.md
+++ b/docs/changelog.md
@@ -96,3 +96,4 @@
 - 将 SysConfig 和启动资源迁入 `board`，IDE 工程元数据迁入 `project`，脚本目录统一为 `tools`。
 - 移除 BSP 对上层的反向依赖：按键和 OLED 改为依赖 `bsp/time`，步进电机状态更新时间改为显式传参，视觉和 IMU 状态由 BSP 接口导出。
 - 更新 CCS 和 Keil 工程路径，使其指向新的分层源码树。
+- 新增独立云台稳定器测试入口 `test_main/gimbal_stabilizer_test_main.c`，去除 OLED 渲染和循环软延时，仅保留 IMU 闭环控制与步进电机限幅输出，默认 yaw 上限保持为 `240 deg/s`。
diff --git a/docs/todo.md b/docs/todo.md
index 91f2aa2..56cff67 100644
--- a/docs/todo.md
+++ b/docs/todo.md
@@ -4,6 +4,7 @@
 - 后续重新设计旧 `Compute_excur()` 对应的转弯期间云台补偿策略，避免在 core 中直接依赖 LED GPIO、旧 `edge/sInedge` 和编码器全局接口。
 - 上板校准 E1 循迹圈数计数策略，重点检查 `LineFollow_IsHalfDetected()` 判定范围、最小距离门限和不同车速下的重复计数问题。
 - 上板校准 `IMU Stabilize` 页面中的 IMU 安装方向、云台轴方向、PID 参数和 pitch 限位。
+- 继续在独立云台稳定器测试入口上调 `yaw_kp`、`yaw_rate_gain` 和加速度限幅，观察去掉 OLED 后的响应变化。
 - 后续根据真实系统时钟复核 `BSP_TIME_CPUCLK_HZ`，必要时改为从时钟配置自动派生。
 - 后续在具体使用点重新设计旧 `sInedge` 和 `UpdateSInedge()` 对应的阶段距离逻辑，优先评估 `Chassis_GetDistance()` / `Chassis_ResetDistance()`。
 - 后续如增加第二个实体按键，在 `KEY_ID`、硬件映射宏和 `bsp/key` 内部配置表中同步扩展。
diff --git a/test_main/gimbal_stabilizer_test_main.c b/test_main/gimbal_stabilizer_test_main.c
index 986e3ea..89e368b 100644
--- a/test_main/gimbal_stabilizer_test_main.c
+++ b/test_main/gimbal_stabilizer_test_main.c
@@ -1,8 +1,8 @@
 #include "bsp_time.h"
 #include "key.h"
+#include "oled.h"
 #include "step_motor.h"
 #include "ti_msp_dl_config.h"
-#include "ui.h"
 #include "wit_sdk.h"
 
 #include <stdbool.h>
@@ -18,18 +18,16 @@
 #define STAB_IMU_ACC_OFFSET 0U
 #define STAB_IMU_GYRO_OFFSET 11U
 #define STAB_IMU_ANGLE_OFFSET 22U
-#define STAB_RENDER_PERIOD_MS 100U
-#define STAB_LOOP_DELAY_MS 2U
 #define STAB_YAW_KP_DEFAULT 8.0f
-#define STAB_YAW_RATE_GAIN_DEFAULT 1.6f
-#define STAB_YAW_LIMIT_DEG_S_DEFAULT 180.0f
-#define STAB_YAW_LIMIT_FAST_DEG_S_DEFAULT 300.0f
-#define STAB_YAW_ACCEL_LIMIT_DEG_S2_DEFAULT 800.0f
-#define STAB_YAW_ACCEL_LIMIT_FAST_DEG_S2_DEFAULT 3200.0f
+#define STAB_YAW_RATE_GAIN_DEFAULT 2.2f
+#define STAB_YAW_LIMIT_DEG_S_DEFAULT 240.0f
+#define STAB_YAW_LIMIT_FAST_DEG_S_DEFAULT 240.0f
+#define STAB_YAW_ACCEL_LIMIT_DEG_S2_DEFAULT 4000.0f
+#define STAB_YAW_ACCEL_LIMIT_FAST_DEG_S2_DEFAULT 12000.0f
 #define STAB_YAW_ACCEL_SWITCH_DEG_DEFAULT 6.0f
 #define STAB_YAW_DEADBAND_DEG_DEFAULT 0.25f
 #define STAB_YAW_RATE_DEADBAND_DEG_S_DEFAULT 1.5f
-#define STAB_YAW_RATE_SIGN_DEFAULT (-1.0f)
+#define STAB_YAW_RATE_SIGN_DEFAULT 1.0f
 
 typedef struct {
     float yaw_kp;
@@ -69,18 +67,22 @@ static uint32_t s_imu_checksum_error_count;
 static uint32_t s_imu_sync_drop_count;
 static uint8_t s_imu_last_byte;
 static uint8_t s_imu_last_frame_type;
+static uint32_t s_imu_gyro_frame_count;
 
 static bool s_reference_ready;
 static WIT_ATTITUDE s_reference_attitude;
 static uint32_t s_last_control_ms;
 static float s_last_yaw_cmd;
+static volatile bool s_reference_pending;
+static volatile bool s_control_pending;
+static uint32_t s_last_control_frame_count;
+static uint32_t s_last_control_gyro_frame_count;
 
 static float s_last_yaw_sample_deg;
 static uint32_t s_last_yaw_sample_ms;
 static uint32_t s_last_yaw_sample_frame_count;
 static float s_yaw_rate_deg_s;
 static bool s_yaw_rate_ready;
-static uint32_t s_last_render_ms;
 static uint32_t s_last_measure_ms;
 
 volatile GIMBAL_STAB_PARAM g_gimbal_stab_param = {
@@ -98,123 +100,6 @@ volatile GIMBAL_STAB_PARAM g_gimbal_stab_param = {
 
 volatile GIMBAL_STAB_DEBUG g_gimbal_stab_debug;
 
-static void GimbalStab_CopyText(char *buffer, uint32_t buffer_size, const char *text){
-    uint32_t index = 0U;
-
-    if (buffer == NULL || buffer_size == 0U){
-        return;
-    }
-
-    if (text != NULL){
-        while (text[index] != '\0' && index < (buffer_size - 1U)){
-            buffer[index] = text[index];
-            index++;
-        }
-    }
-
-    buffer[index] = '\0';
-}
-
-static void GimbalStab_AppendChar(char *buffer, uint32_t buffer_size, char ch){
-    uint32_t index = 0U;
-
-    if (buffer == NULL || buffer_size == 0U){
-        return;
-    }
-
-    while (buffer[index] != '\0' && index < (buffer_size - 1U)){
-        index++;
-    }
-
-    if (index < (buffer_size - 1U)){
-        buffer[index] = ch;
-        buffer[index + 1U] = '\0';
-    }
-}
-
-static void GimbalStab_AppendU32(char *buffer, uint32_t buffer_size, uint32_t value){
-    char digits[10];
-    uint8_t digit_count = 0U;
-
-    if (value == 0U){
-        GimbalStab_AppendChar(buffer, buffer_size, '0');
-        return;
-    }
-
-    while (value > 0U && digit_count < sizeof(digits)){
-        digits[digit_count++] = (char)('0' + (value % 10U));
-        value /= 10U;
-    }
-
-    while (digit_count > 0U){
-        digit_count--;
-        GimbalStab_AppendChar(buffer, buffer_size, digits[digit_count]);
-    }
-}
-
-static void GimbalStab_AppendI32(char *buffer, uint32_t buffer_size, int32_t value){
-    uint32_t abs_value;
-
-    if (value < 0){
-        GimbalStab_AppendChar(buffer, buffer_size, '-');
-        abs_value = (uint32_t)(-value);
-    } else{
-        abs_value = (uint32_t)value;
-    }
-
-    GimbalStab_AppendU32(buffer, buffer_size, abs_value);
-}
-
-static void GimbalStab_FormatU32(char *buffer, uint32_t buffer_size, const char *prefix, uint32_t value){
-    GimbalStab_CopyText(buffer, buffer_size, prefix);
-    GimbalStab_AppendU32(buffer, buffer_size, value);
-}
-
-static int32_t GimbalStab_FloatToDeci(float value){
-    if (value >= 0.0f){
-        return (int32_t)((value * 10.0f) + 0.5f);
-    }
-
-    return (int32_t)((value * 10.0f) - 0.5f);
-}
-
-static int32_t GimbalStab_FloatToInt(float value){
-    if (value >= 0.0f){
-        return (int32_t)(value + 0.5f);
-    }
-
-    return (int32_t)(value - 0.5f);
-}
-
-static void GimbalStab_FormatDeci(char *buffer, uint32_t buffer_size, const char *prefix, float value){
-    int32_t deci = GimbalStab_FloatToDeci(value);
-    int32_t whole;
-    int32_t frac;
-
-    GimbalStab_CopyText(buffer, buffer_size, prefix);
-
-    if (deci < 0){
-        whole = (-deci) / 10;
-        frac = (-deci) % 10;
-        GimbalStab_AppendChar(buffer, buffer_size, '-');
-        GimbalStab_AppendU32(buffer, buffer_size, (uint32_t)whole);
-        GimbalStab_AppendChar(buffer, buffer_size, '.');
-        GimbalStab_AppendU32(buffer, buffer_size, (uint32_t)frac);
-        return;
-    }
-
-    whole = deci / 10;
-    frac = deci % 10;
-    GimbalStab_AppendU32(buffer, buffer_size, (uint32_t)whole);
-    GimbalStab_AppendChar(buffer, buffer_size, '.');
-    GimbalStab_AppendU32(buffer, buffer_size, (uint32_t)frac);
-}
-
-static void GimbalStab_FormatInt(char *buffer, uint32_t buffer_size, const char *prefix, float value){
-    GimbalStab_CopyText(buffer, buffer_size, prefix);
-    GimbalStab_AppendI32(buffer, buffer_size, GimbalStab_FloatToInt(value));
-}
-
 static float GimbalStab_Clamp(float value, float limit){
     if (value > limit){
         return limit;
@@ -313,6 +198,7 @@ static void GimbalStab_PollDebugUart(void){
                     break;
                 case STAB_IMU_FRAME_GYRO:
                     offset = STAB_IMU_GYRO_OFFSET;
+                    s_imu_gyro_frame_count++;
                     break;
                 case STAB_IMU_FRAME_ANGLE:
                     offset = STAB_IMU_ANGLE_OFFSET;
@@ -328,6 +214,17 @@ static void GimbalStab_PollDebugUart(void){
             s_imu_last_frame_type = s_imu_rx_buffer[1];
             s_imu_frame_count++;
             GYROSCOPE_DATA_Decoder(s_imu_decode_buffer);
+
+            if ((s_imu_last_frame_type == STAB_IMU_FRAME_ANGLE) ||
+                (s_imu_last_frame_type == STAB_IMU_FRAME_GYRO)){
+                if (!s_reference_ready){
+                    if (s_imu_angle_frame_count > 0U){
+                        s_reference_pending = true;
+                    }
+                } else{
+                    s_control_pending = true;
+                }
+            }
         } else{
             s_imu_checksum_error_count++;
         }
@@ -342,18 +239,23 @@ static void GimbalStab_StopOutput(void){
     s_last_yaw_cmd = 0.0f;
 }
 
+static void GimbalStab_DisableOled(void){
+    OLED_DisplayOff();
+}
+
 static void GimbalStab_AlignReference(void){
     WIT_ATTITUDE attitude = {0};
 
     (void)WitGetAttitude(&attitude);
     s_reference_attitude = attitude;
     s_last_control_ms = BSP_Time_GetMs();
+    s_last_control_frame_count = s_imu_angle_frame_count;
+    s_last_control_gyro_frame_count = s_imu_gyro_frame_count;
     s_last_yaw_cmd = 0.0f;
     s_reference_ready = true;
     s_yaw_rate_ready = false;
     s_yaw_rate_deg_s = 0.0f;
     s_last_yaw_sample_frame_count = s_imu_angle_frame_count;
-    s_last_render_ms = s_last_control_ms;
     s_last_measure_ms = s_last_control_ms;
     GimbalStab_StopOutput();
     StepMotor_ResetEstimatedPosition(STEP_MOTOR_CHANNEL_YAW);
@@ -371,15 +273,23 @@ static void GimbalStab_ApplyControl(void){
     (void)WitGetData(&imu);
     GimbalStab_UpdateYawRate(&imu.attitude_deg, now_ms);
 
-    float dt_s = (float)(now_ms - s_last_control_ms) / 1000.0f;
-    if (dt_s <= 0.0f){
-        dt_s = 0.001f;
+    uint32_t angle_frame_delta = s_imu_angle_frame_count - s_last_control_frame_count;
+    uint32_t gyro_frame_delta = s_imu_gyro_frame_count - s_last_control_gyro_frame_count;
+    uint32_t frame_delta = (gyro_frame_delta > angle_frame_delta) ? gyro_frame_delta : angle_frame_delta;
+    float dt_s = 0.002f;
+    if (frame_delta > 0U){
+        dt_s = 0.002f * (float)frame_delta;
     }
     s_last_control_ms = now_ms;
+    s_last_control_frame_count = s_imu_angle_frame_count;
+    s_last_control_gyro_frame_count = s_imu_gyro_frame_count;
 
     float yaw_delta_deg = GimbalStab_WrapAngleDeg(imu.attitude_deg.yaw - s_reference_attitude.yaw);
     float yaw_error_deg = -yaw_delta_deg;
-    float yaw_rate_deg_s = s_yaw_rate_deg_s;
+    float gyro_yaw_rate_deg_s = imu.gyro_deg_s.z;
+    float yaw_rate_deg_s = (GimbalStab_Abs(gyro_yaw_rate_deg_s) > 0.1f) ?
+        gyro_yaw_rate_deg_s :
+        s_yaw_rate_deg_s;
     float yaw_damping = g_gimbal_stab_param.yaw_rate_sign *
         yaw_rate_deg_s *
         g_gimbal_stab_param.yaw_rate_gain;
@@ -405,6 +315,7 @@ static void GimbalStab_ApplyControl(void){
     float yaw_cmd = GimbalStab_SlewRate(s_last_yaw_cmd,
                                         yaw_target_cmd,
                                         accel_limit * dt_s);
+    yaw_cmd = GimbalStab_Clamp(yaw_cmd, yaw_limit);
 
     s_last_yaw_cmd = yaw_cmd;
     g_gimbal_stab_debug.yaw_deg = imu.attitude_deg.yaw;
@@ -419,51 +330,24 @@ static void GimbalStab_ApplyControl(void){
     g_gimbal_stab_debug.imu_angle_frame_count = s_imu_angle_frame_count;
     g_gimbal_stab_debug.control_ms = now_ms;
     g_gimbal_stab_debug.flags = (s_reference_ready ? 0x01U : 0U) |
-        (s_yaw_rate_ready ? 0x02U : 0U);
+        (s_yaw_rate_ready ? 0x02U : 0U) |
+        ((GimbalStab_Abs(gyro_yaw_rate_deg_s) > 0.1f) ? 0x04U : 0U);
     (void)StepMotor_SetSpeed(STEP_MOTOR_CHANNEL_YAW, yaw_cmd);
     (void)StepMotor_SetSpeed(STEP_MOTOR_CHANNEL_PITCH, 0.0f);
 }
 
-static void GimbalStab_Render(void){
-    WIT_ATTITUDE attitude = {0};
-    uint32_t now_ms = BSP_Time_GetMs();
-    float yaw_delta_deg;
-    char line0[24];
-    char line1[24];
-    char line2[24];
-    char line3[24];
-    char line4[24];
-    char line5[24];
-
-    (void)WitGetAttitude(&attitude);
-    (void)StepMotor_UpdateAllState(now_ms);
-
-    yaw_delta_deg = GimbalStab_WrapAngleDeg(attitude.yaw - s_reference_attitude.yaw);
-
-    GimbalStab_FormatDeci(line0, sizeof(line0), "Yaw:", attitude.yaw);
-    GimbalStab_FormatDeci(line1, sizeof(line1), "YawV:", s_yaw_rate_deg_s);
-    GimbalStab_FormatDeci(line2, sizeof(line2), "Err:", yaw_delta_deg);
-    GimbalStab_FormatInt(line3, sizeof(line3), "Cmd:", s_last_yaw_cmd);
-    GimbalStab_FormatInt(line4, sizeof(line4), "Lim:", g_gimbal_stab_param.yaw_limit_deg_s);
-    GimbalStab_FormatDeci(line5, sizeof(line5), "Ref:", s_reference_attitude.yaw);
-
-    Ui_RenderLines("Gimbal Stabilizer",
-                   line0,
-                   line1,
-                   line2,
-                   line3,
-                   line4,
-                   line5);
-}
-
 static void GimbalStab_InitSystems(void){
     SYSCFG_DL_init();
     BSP_Time_Init();
     __enable_irq();
 
-    Ui_Init();
+    GimbalStab_DisableOled();
     Key_Init();
     (void)StepMotor_Init();
+    s_reference_pending = false;
+    s_control_pending = false;
+    s_last_control_frame_count = 0U;
+    s_last_control_gyro_frame_count = 0U;
 
     DL_TimerA_startCounter(TIMER_0_INST);
     DL_UART_Main_enableInterrupt(Debug_INST, DL_UART_MAIN_INTERRUPT_RX);
@@ -474,54 +358,22 @@ static void GimbalStab_InitSystems(void){
 
 int main(void){
     GimbalStab_InitSystems();
-    Ui_RenderLines("Gimbal Stabilizer",
-                   "Waiting IMU",
-                   "OLED OK",
-                   NULL,
-                   NULL,
-                   NULL,
-                   NULL);
-
-    while (!s_reference_ready){
-        GimbalStab_PollDebugUart();
-        if (s_imu_angle_frame_count > 0U){
-            GimbalStab_AlignReference();
-        }
-        if ((BSP_Time_GetMs() - s_last_render_ms) >= 500U){
-            char line0[24];
-            char line1[24];
-
-            GimbalStab_FormatU32(line0, sizeof(line0), "Frm:", s_imu_frame_count);
-            GimbalStab_FormatU32(line1, sizeof(line1), "Byte:", s_imu_byte_count);
-            Ui_RenderLines("Gimbal Stabilizer",
-                           "Waiting IMU",
-                           line0,
-                           line1,
-                           NULL,
-                           NULL,
-                           NULL);
-            s_last_render_ms = BSP_Time_GetMs();
-        }
-        BSP_DelayMs(2U);
-    }
-
-    GimbalStab_Render();
-    s_last_render_ms = BSP_Time_GetMs();
 
     while (1){
-        GimbalStab_PollDebugUart();
-
         if (Key_IsShortPress(KEY_ID_1)){
             Key_ClearAllEvents();
             GimbalStab_AlignReference();
         }
 
-        GimbalStab_ApplyControl();
-        if ((BSP_Time_GetMs() - s_last_render_ms) >= STAB_RENDER_PERIOD_MS){
-            GimbalStab_Render();
-            s_last_render_ms = BSP_Time_GetMs();
+        if (s_reference_pending){
+            s_reference_pending = false;
+            GimbalStab_AlignReference();
+        }
+
+        if (s_control_pending){
+            s_control_pending = false;
+            GimbalStab_ApplyControl();
         }
-        BSP_DelayMs(STAB_LOOP_DELAY_MS);
     }
 }
 
@@ -535,7 +387,6 @@ void SysTick_Handler(void){
     static uint8_t scan_divider = 0U;
 
     BSP_Time_TickInc();
-    GimbalStab_PollDebugUart();
 
     scan_divider++;
     if (scan_divider >= 10U){
